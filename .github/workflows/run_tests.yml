---
name: run_tests

on:
  push:
    branches:
      - "**"
  pull_request:

concurrency:
  group: >-
    ${{ github.workflow }}-
    ${{ github.event.pull_request.head.sha || github.sha }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
